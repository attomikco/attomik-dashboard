# Supabase — get these from supabase.com > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Shopify — add per client via Settings UI, stored in organizations table
# These are fallback/dev values only
SHOPIFY_ACCESS_TOKEN=
SHOPIFY_STORE_DOMAIN=

# Meta Ads
META_ACCESS_TOKEN=
META_AD_ACCOUNT_ID=

# Weekly Email — when set, ALL weekly emails go to this address instead of org members
WEEKLY_EMAIL_OVERRIDE=
